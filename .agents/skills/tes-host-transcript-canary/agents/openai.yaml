interface:
  display_name: "TES Host Transcript Canary"
  short_description: "Run scripted transcript canary loops."
  default_prompt: "Use $tes-host-transcript-canary to run a scripted host transcript canary loop, record sanitized ledger evidence, and decide the next corrective step."
