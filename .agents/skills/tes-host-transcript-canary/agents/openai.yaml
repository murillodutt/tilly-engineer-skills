interface:
  display_name: "TES Host Transcript Canary"
  short_description: "Run transcript-backed canary loops."
  default_prompt: "Use $tes-host-transcript-canary to run a persistent host-backed canary loop, audit the sanitized Claude transcript, and decide the next corrective step."
