{
  "name": "tilly-engineer-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Portable engineering discipline and context-mesh method for coding agents.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "validate": "python3 scripts/validate_reference_package.py",
    "oracle:self-test": "python3 .agents/skills/tilly-engineering-discipline/scripts/discipline_oracle.py --self-test",
    "benchmark:plan": "python3 scripts/context_mesh_plan.py",
    "git:diff-check": "git diff --check && git diff --cached --check",
    "commit:check": "python3 scripts/validate_reference_package.py && python3 scripts/context_mesh_plan.py && git diff --check && git diff --cached --check"
  }
}
