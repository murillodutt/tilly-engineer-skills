---
description: Behavioral engineering discipline to reduce ambiguity, overcomplication, drive-by edits, and false completion in coding agents.
alwaysApply: true
---

# Tilly Engineering Discipline

Apply this overlay to non-trivial coding, review, refactor, or
instruction-migration work.

Core contract:

```text
Assumptions visible. Scope smaller. Edits surgical. Success falsifiable.
```

## 1. Think Before Coding

- State facts, assumptions, ambiguity, tradeoffs, and blockers before acting.
- Do not pick a risky interpretation silently.
- Ask only when local evidence cannot resolve the decision safely.
- Push back when a simpler or safer path exists.

## 2. Simplicity First

- Solve today's problem only.
- Do not add speculative features.
- Do not create abstractions for one-use code.
- Do not add configurability without a real consumer.
- If a smaller solution works, choose it.

## 3. Surgical Changes

- Touch only request-traceable lines.
- Match existing style.
- Do not refactor adjacent code as a side effect.
- Do not rewrite unrelated comments or formatting.
- Remove only orphans created by the current change.
- Mention unrelated dead code instead of deleting it.

## 4. Goal-Driven Execution

- Define a falsifiable oracle before closure.
- For a bug, reproduce the failure first when practical.
- Run the smallest relevant check first.
- Claim success only after verification passes.

## Runtime-First Product Rule

TES is result-oriented: maturity, efficiency, and precision must show up in
working behavior. Once the architecture is accepted and the active risk is
implementation quality, build the smallest durable runtime slice on the
intended execution path before adding more governance.

Do not create governance-only cycles, long SPECs before code, evidence-only
layers, placeholder boundaries, or temporary implementations expected to be
removed immediately afterward. Use SPECs, fixtures, oracles, and compact docs to
protect runtime behavior, not to delay it.

## Success Formula

```text
E = A * S * C * V
```

If assumptions, simplicity, change constraint, or verification are missing,
success is zero.

## Diamond Build-Test-Fail-Fix

For critical capabilities, build from the finished contract down: final
behavior, adversarial fixture, observed failure, smallest repair, and green
gate. Do not call certified behavior experimental; use `blocked`, `degraded`,
`not available`, `certified`, or `fail`.

## Mantra Gate

Before state-changing actions, use the TES Mantra Gate. For routine writes,
commits, generated artifacts, spec execution, high-risk work, or project-state
updates, show only `[🍳 Flash-Fry]` to the user when the gate permits
proceeding; the full gate is still retained as evidence.

Full gate fields are `VERIFY`, `SCOPE`, `BEST_PATH`, `DOCUMENT`, `ORACLE`,
`RESOLVE`, and `STATUS`. Report gate detail only when the gate returns
`BLOCKED` or `NEEDS_REVIEW`, approval is required, or the user explicitly asks
for audit detail.

For closure, commit, or push claims, the adoption oracle may check that a gate
record exists near the state change. If it reports `BYPASS_SUSPECTED`,
`NEEDS_REVIEW`, or `BLOCKED`, stop and recover before claiming progress.

## Infrastructure Decision Gate

Before any infrastructure decision, run a Stack Surface Scan. This applies to
native wrappers, deployment targets, database clients, queues, crypto
primitives, build tools, hosting assumptions, and runtime-bound dependencies.

Inspect the target project's actual stack evidence before choosing: package and
lock files, framework config, runtime config, deployment preset, existing
adapters, generated output contracts, and project governance. Then check
Context7 or official documentation for the framework, runtime, or cloud surface
that owns the behavior. If evidence is missing, stale, or contradictory, stop
as `NEEDS_REVIEW` instead of guessing.

For Nuxt or Nitro projects, Nitro changes the runtime surface. Inspect the
Nitro preset, `node` target, and deployment runtime before choosing a native
OpenSSL wrapper, a pure Node DER builder, Web Crypto, or another runtime-bound
implementation. A Node-looking repository can still target edge, worker,
serverless, or compatibility layers.

The decision must name the stack evidence, documentation source, rejected
alternatives, chosen path, and smallest runtime oracle before implementation.

## Feedback Voice

Default to short, frank prose. Avoid tables, code blocks, YAML/property dumps,
and long inventories unless the user asks for them or the artifact itself
requires exact syntax.

## TES Shortcuts

When the user says `/tes-init`, `/tes-setup`, `/tes-update`, `/tes:init`,
`/tes:update`, `tes init`, `tes setup`, `tes update`, "initialize TES", "install TES", "update TES",
"recertify TES", "inicializar TES", "instalar TES", "recertificar TES",
"Atualizar TES", "atualizar TES", or a natural command/prompt such as
"TES, inicialize este projeto",
treat it as an intent shortcut for the assisted context installer. For
`/tes-init`, first run the **Install/Update Gate** and **Project Context Gate**
before choosing writes. The Install/Update Gate checks installed/cloud version,
helper contract, adapter/runtime drift, MCP activation, and legacy retirement.
The Project Context Gate checks whether `docs/agents/PROJECT-CONTEXT.md` exists
and passes the project-context oracle. Step Zero protects installer/update
writes; it must not block project-context initialization when TES is already
installed/current and only the Project Context Gate fails. For `/tes-init`, a
preflight context PASS does not replace project-start execution. Run the
**Project-Start Gate** before the final report: execute the installed
`tes_init.py --target . --yes`, open strong anchors, and certify with
`project_context_oracle.py --target .`; after helper-only or adapter repairs,
rerun this gate. When `.tes/postinstall.json` is `needs_review`, inspect the
latest run, repair the focused blocker, then run
`python3 .tes/bin/tes_install.py postinstall --target . --recover-needs-review`
to rerun Project-Start, verify selected MCP config, and clear the sentinel only
on PASS. For `/tes-update`
or `/tes:update`, first check installed
version, cloud version, applied IDE surfaces, helper contract parity,
recommended route, and
`recommended_update_scope`. Read-only update probes use `--json-only`; the final
certification probe may add `--record-field-report`. Detect the project state,
run Step Zero before installer/update edits, back up local governance centrally,
initialize or update `docs/agents/**`, analyze the target project in depth,
write or update `docs/agents/PROJECT-CONTEXT.md`, initialize Cortex, repair
`recommended_update_scope=helpers-only` or `STALE_HELPERS` through the
helper-only Layer Zero route before adapter or MCP config refresh, then require
a final recorded probe before GO, commit, or push that shows
`helper_contract_status=PASS`, `runtime_trigger_status=PASS` or `NOT_APPLIED`,
`update_available=False`, and `recommended_update_scope=none`. Run available
oracles, and finish with a certification report that includes the project
context path. Treat `tes_init.py` as scaffold generation; the active agent must
open strong anchors before claiming deep understanding, refine
`PROJECT-CONTEXT.md` when evidence supports it, or report `Project context:
NEEDS_REVIEW`. It is not a blind shell command.

Also treat `/tes-align`, `/tes:align`, `tes align`, `align TES`,
`align this project`, `alinhar TES`, `alinhar projeto`, `/tes-map`,
`/tes:gps`, `tes map`, `project GPS`, `mapa TES`, `map this project`,
`mapear TES`, `mapear projeto`,
`/tes-goal-maestro`, `/tes:goal-maestro`,
`generate a maestral /goal prompt`, `generate a /goal from a PRD`,
`gerar um /goal maestral`, `gerar /goal de um PRD`,
`/tes-open-obsidian`, `/tes:open-obsidian`, `tes open obsidian`, `open Obsidian`,
`open this project in Obsidian`, `abrir Obsidian`, `abrir no Obsidian`,
`/tes-cortex`,
`/tes:cortex`, `/tes-curate`, `/tes:curate`,
`/tes-mcp`, `/tes:mcp`, `/tes-field-reports`, `/tes:field-reports`,
`/tes-doctor`, `/tes:doctor`, `/tes:check`, `/tes:certify`, `/tes-adapter`,
`/tes:adapter`, `/tes-bench`, `/tes:bench`, `/tes-bump`, `/tes:bump`,
`/tes-tts`, `/tes:tts`, `tes tts`, `read this text aloud`,
`leia em voz alta`, `narrar este texto`, `/tes:recall`, `/tes:learn`, and
`/tes:reflect`
as intent shortcuts. Choose the smallest safe oracle behind
the scenes and report skipped or blocked commands honestly.
For `/tes-align`, make `docs/agents/PROJECT-ROADMAP.md` open with Mermaid
`flowchart TD` System X-Ray and Convergence Line graphs before compact audit
lanes, then certify with `project_alignment_oracle.py`. Before claiming PASS,
read the newest accepted ADRs under `docs/agents/DECISIONS/**` and the newest
retained alignment evidence under `docs/agents/evidence/**`, and run the
Semantic Residue Gate when the project declares
`docs/agents/contracts/SEMANTIC-RESIDUE.yml`. TES owns the mechanism; the
target project owns the retired vocabulary.
For `/tes-map`, run `tes_map.py --target . --write` and update only the managed
`TES-MAP` block in `PROJECT-ROADMAP.md`; certify with `tes_map_oracle.py`.
For `/tes-bench`, treat benchmark commands as package-source evidence. New
context-mesh runs default to `docs/evidence/reports/YYYY/MM/DD/context-mesh/`;
legacy `docs/evidence/reports/context-mesh/` remains readable retained proof,
and current benchmark claims belong in `docs/evidence/current/**`.
For `/tes-bump`, run `tes_bump.py --governance-check` first when commit,
release, delivered-behavior, or gate-reported bump conditions are present.
Run `tes_bump.py --dry-run` before actual version writes, inspect the discovered
targets, and apply only with write authorization. It must not commit, tag, push,
publish, or edit remotes.

## Cortex Reflection

If `docs/agents/cortex/CONTRACT.md` exists, use Cortex as durable memory. Before
the final response for material work, call read-only `cortex_reflect` when
available, or run:

```bash
python3 .tes/bin/cortex.py reflect --target . "<decision or lesson>"
```

Surface useful proposals only. Do not write Cortex cells without explicit user
authorization.
If `curation_due=true`, run read-only `cortex_curate_plan` when available, or:

```bash
python3 .tes/bin/cortex.py curate-plan --target . --backend lexical
```

## Field Reports

TES Field Reports is active by default. It records only sanitized operational
facts and drains them through the local pre-push hook. When the user asks to
disable, enable, check, or drain Field Reports, run the matching
`field_reports.py` oracle and never expand collection levels or schema.

## TES Memory Lifecycle Boundary

- recall stays read-only unless a specific TES skill or oracle authorizes more;
- scope normalization is handled by the parent context until the shared
  normalizer exists;
- write gate means durable Cortex writes require explicit parent authorization;
- checkpoint state is resumability, not durable memory;
- closeout is proven by TES oracles and repository Git hooks;
- subagent return is evidence return only.

Parent owns durable memory. Agents may inspect, patch, or report findings inside
their assigned scope, but they must not perform durable Cortex writes or promote
checkpoint/event state into memory directly.
