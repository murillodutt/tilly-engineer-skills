---
tds_id: evidence.cortex_mcp_host_segmentation_20260527
tds_class: evidence
status: active
consumer: Cortex MCP authors, installer authors, host integration authors, release reviewers, and operators
source_of_truth: false
evidence_level: L2
tver: 0.1.0
---

# Cortex MCP Host Segmentation Wave Closure

Date: 2026-05-27.

## Summary

TES executes the four serial waves planned in
`docs/roadmap/GOAL-SUPER-SPEC-cortex-mcp-host-segmentation.md`. The Cortex MCP
installer at `scripts/install_mcp.py` becomes a host-agnostic orchestrator that
delegates to per-host adapter modules under `scripts/install_mcp_hosts/**`.
Each adapter owns one host's exact schema for stdio, opt-in localhost HTTP,
strict per-host validation, and bearer-env authenticated HTTP.

The Cortex MCP server contract from ADR 0003 is unchanged. The
`cortex_remember_plan` / `cortex_remember` write lane is unchanged. No tool
schemas, prompts, resources, or transports were added or removed at the server
layer. Markdown under `docs/agents/cortex/**` remains durable memory truth.

## Wave Results

| Wave | Outcome | Owned files |
|------|---------|-------------|
| SPEC-000 | PASS | Super SPEC, EXECUTION-UNITS companion, three index updates |
| SPEC-001 | PASS | `install_mcp_hosts/__init__.py`, `base.py`, four host skeletons |
| SPEC-002 | PASS | Codex adapter migrated with byte-identical TOML output |
| SPEC-003 | PASS | Claude and Cursor adapters migrated with byte-identical JSON output |
| SPEC-004 | PASS | VS Code adapter migrated; consolidated per-host golden tests landed |
| SPEC-005 | PASS | `build_http` for all four hosts, `--transport http --port --host --url --allow-non-localhost` flags |
| SPEC-006 | PASS | `allowed_fields()` and `forbidden_fields()` per host with pre-write strict validation |
| SPEC-007 | PASS | `--bearer-env` flag with per-host interpolation and privacy self-test |

## Per-Host Field Coverage Generated By The Installer

| Host | stdio fields | http fields | bearer interpolation |
|------|--------------|-------------|----------------------|
| Codex | `command`, `args`, `cwd`, `startup_timeout_sec`, `tool_timeout_sec`, `enabled` | `url`, `startup_timeout_sec`, `tool_timeout_sec`, `enabled` | `bearer_token_env_var = "<NAME>"` |
| Claude | `type`, `command`, `args`, `env` | `type`, `url`, `headers` | `headers.Authorization = "Bearer ${env:<NAME>}"` |
| Cursor | `type`, `command`, `args`, `env` | `type`, `url`, `headers` | `headers.Authorization = "Bearer ${env:<NAME>}"` |
| VS Code | `type`, `command`, `args`, `env` | `type`, `url`, `headers` | `headers.Authorization = "Bearer ${input:<name>-token}"` |

Forbidden-field guards rejected before write:

- Codex stdio rejects `url`, `bearer_token_env_var`, `http_headers`, `env_http_headers`.
- Codex http rejects `command`, `args`, `env`, `cwd`.
- Claude (any transport) rejects `cwd`.
- Cursor http rejects `command`, `args`, `env`, `cwd`.
- VS Code rejects unknown fields under either transport.

Codex `deny_unknown_fields` parity is enforced by `assert_entry_valid` against
the closed `McpServerConfig` and `StreamableHttp` field sets mirrored from
`codex-rs/config/src/mcp_types.rs`.

## Oracles

All Gate Record oracles plus the full `commit:check` suite passed at SHA
`b1fffd5cda978b880bfc7b07fee8aa87e9d791df` working-tree state:

| Oracle | Result |
|--------|--------|
| `python3 scripts/validate_tds.py` | PASS — 139 documents |
| `python3 scripts/validate_doc_size.py` | PASS — Super SPEC at 431 lines |
| `python3 scripts/validate_reference_graph.py` | PASS — 210 checked files |
| `git diff --check` | PASS |
| `python3 scripts/cortex_mcp.py --self-test` | PASS — server contract unchanged |
| `python3 scripts/install_mcp.py --self-test` | PASS — stdio + http + bearer + non-localhost rejection + golden per host |
| `python3 scripts/install_smoke.py --self-test` | PASS |
| `python3 scripts/platform_surface_oracle.py --self-test` | PASS — updated to look up host adapters under `install_mcp_hosts/**` |
| `python3 scripts/tes_init.py --self-test` | PASS |
| `python3 scripts/tes_update.py --self-test` | PASS |
| `python3 scripts/tes_bundle.py --self-test` | PASS — HELPER_FILES includes `install_mcp_hosts/**` |
| `npm run commit:check` | PASS — 45 oracles end-to-end |

## CLI Surface

The orchestrator surface remains backward-compatible and gains four flags:

```text
--transport stdio|http        # default stdio
--host <ip>                   # default 127.0.0.1
--port <int>                  # default 8765
--url <url>                   # explicit override; rejects non-localhost unless allowed
--allow-non-localhost         # required for any non-loopback host
--bearer-env <NAME>           # env var name for bearer; value never read or stored
```

Existing flags (`--read-only`, `--enable-writes`, `--helpers-only`,
`--overwrite`, `--no-backup`, `--dry-run`, `--yes`, `--json-only`,
`--self-test`) keep their existing semantics.

## Privacy Self-Test

The bearer-env self-test exports `TES_BEARER_TOKEN=super-secret-not-stored`
into the install subprocess environment, runs `--transport http --bearer-env
TES_BEARER_TOKEN --adapter all`, and asserts:

- the secret value never appears in stdout or stderr;
- the secret value never appears in any generated `.codex/config.toml`,
  `.mcp.json`, `.cursor/mcp.json`, or `.vscode/mcp.json`;
- the secret value never appears in the `.tes/events/ledger.jsonl` event
  written by `field_reports.safe_record_event`;
- the Authorization header in every JSON host references the env or input
  variable, not the literal secret;
- Codex emits `bearer_token_env_var = "TES_BEARER_TOKEN"` and never the value.

## Boundary Statement

This closure does not change ADR 0001 or ADR 0003. It does not add new MCP
tools, prompts, resources, or transports at the server. It does not weaken
the `cortex_remember` plan-then-apply lane. It does not mutate any global host
configuration, secret, hook, remote, package lockfile, `.obsidian/**`, or
Cortex source material. Non-localhost HTTP installs require explicit
`--allow-non-localhost`, mirroring the server-side guard.

## Residual Items

- Codex CLI host recognition (`codex mcp list`) for the new HTTP variant is
  not yet exercised under remote release certification; only local
  package-source self-tests cover the registration shape.
- Cursor `auth` block emission for OAuth-style flows is documented as a
  capability but not exercised by the current self-test; only inline
  `headers.Authorization` interpolation is covered.
- Wave 2–4 are delivered behavior. Release identity classification remains
  the operator's call at the next package bump.
